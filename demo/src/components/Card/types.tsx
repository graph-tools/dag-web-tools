export interface CardProps {
  className?: string;
}

export interface DemoSectionProps {
  status?: string;
  disabled?: boolean;
  onClick?: () => void;
}

export interface DemoParamProps {
  done?: boolean;
  required?: boolean;
}
